import React from 'react';
import {Links, Form} from "../components"

const Register = () => {
    return (
        <div>
            <Links />
            <Form />
        </div>
    );
}

export default Register;
